
name: Build and test

on:
  pull_request:
    branches: [ * ]
  push:
    branches: [ * ]

jobs:
  build-and-test:
    uses: GTNewHorizons/GTNH-Actions-Workflows/.github/workflows/build-and-test.yml@master
    secrets: inherit
